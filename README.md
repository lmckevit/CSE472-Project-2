In order to generate the data sets for the simulation run the code in the DatasetCreation folder.
There is a seperate Readme in that folder which details how to run that code

Once you have the dataset in your bucket in Google cloud projects you can run the code in the Simulation folder
There is a seperate Readme in that folder as well to detail how to run that code